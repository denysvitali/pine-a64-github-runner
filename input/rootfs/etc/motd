Pine A64 GitHub Actions runner

This login always opens a recovery shell; onboarding is never forced.
Run `doas /usr/local/sbin/gha-setup` manually to configure the device.
For a root recovery shell, run `doas -s` and enter the admin password.
