#!/bin/sh
# Always provide a recovery shell. Setup is explicitly opt-in.
if [ ! -f /data/conf/onboarding-complete ]; then
    cat <<'EOF'

Pine A64 runner setup is not complete.
Run `doas gha-setup` when ready, or continue using this recovery shell.

EOF
fi

exec /bin/ash "$@"
